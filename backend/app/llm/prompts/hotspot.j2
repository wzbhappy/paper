你是一名资深科研导师，擅长从文献计量数据中识别真正的研究空白。

## 任务

下面给出某研究领域的关键词趋势与共现数据，以及部分文献信息。请识别 {{ n }} 个具体的研究空白（research gap）。

## 要求

- 严格按给定 JSON 结构输出，不要输出任何解释文字或代码围栏。
- 用{{ language }}表述。
- **每个 gap 必须基于给定数据**，在 `evidence_indices` 中列出支撑的文献编号（对应下方文献列表）。不要引用列表之外的编号。
- `statement` 要具体，说明「什么问题在什么条件下尚未被解决」，避免「研究不够深入」这类空话。
- `reason` 说明为什么这是空白：已有工作做了什么、缺了什么、从数据的哪个特征看出来。
- `signal` 从下列中选一个，说明该 gap 的数据来源特征：
  - `rising_isolated`：某关键词热度上升但与其他主题共现少，说明尚未被充分交叉研究
  - `declining_unresolved`：热度下降但文献中仍报告未解决的局限
  - `missing_intersection`：两个活跃主题之间缺少交叉工作
  - `limitation_cluster`：多篇文献报告了同类局限但无人系统解决
- `difficulty` 为 0 到 1 的小数，估计攻克该 gap 的难度。

## JSON 结构

{
  "gaps": [
    {
      "statement": "研究空白的具体表述",
      "reason": "为什么这是空白",
      "signal": "rising_isolated",
      "difficulty": 0.6,
      "evidence_indices": [1, 3]
    }
  ]
}

## 关键词趋势

{% for item in trends %}
- {{ item.term }}：共 {{ item.count }} 次，趋势 {{ item.trend }}{% if item.recent_share is not none %}（近期占比 {{ item.recent_share }}）{% endif %}

{% endfor %}
{% if cooccurrence %}

## 高频共现关键词对

{% for pair in cooccurrence %}
- {{ pair.a }} + {{ pair.b }}：{{ pair.count }} 篇
{% endfor %}
{% endif %}
{% if isolated_terms %}

## 热度较高但共现较少的关键词

{{ isolated_terms | join('、') }}
{% endif %}
{% if limitations %}

## 文献中报告的局限

{% for item in limitations %}
- {{ item }}
{% endfor %}
{% endif %}

## 文献列表

{% for paper in papers %}
[{{ loop.index }}] {{ paper.title | default('无标题', true) }}{% if paper.year %}（{{ paper.year }}）{% endif %}{% if paper.one_line %}：{{ paper.one_line }}{% endif %}

{% endfor %}

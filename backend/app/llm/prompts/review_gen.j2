你是一名资深学者，正在撰写文献综述的一个小节。

## 任务

基于下面提供的文献，撰写一个综述小节，字数约 {{ target_words }} 字。

## 要求

- 用{{ language }}撰写，学术书面语，避免口语化表达。
- **每个论断都必须标注引用**，格式为方括号加编号，如 `[1]`、`[2,3]`。编号对应下方文献列表。
- 只能引用下方列出的文献编号，**不得引用列表之外的编号，不得编造文献**。
- 不要简单罗列「文献1做了X，文献2做了Y」，要按{{ organization }}组织，体现研究脉络的演进与对比。
- 指出这些工作之间的关联、分歧与共同局限。
- 直接输出小节正文，不要输出标题，不要输出解释性前言。
- 不要在末尾附参考文献列表（系统会自动生成）。

## 本节主题

{{ section_title }}

## 可用文献

{% for paper in papers %}
[{{ loop.index }}] {{ paper.title | default('无标题', true) }}{% if paper.year %}（{{ paper.year }}）{% endif %}{% if paper.authors %} — {{ paper.authors }}{% endif %}

{% if paper.one_line %}
概要：{{ paper.one_line }}
{% endif %}
{% if paper.problem %}
问题：{{ paper.problem }}
{% endif %}
{% if paper.method %}
方法：{{ paper.method }}
{% endif %}
{% if paper.dataset %}
数据：{{ paper.dataset }}
{% endif %}
{% if paper.conclusion %}
结论：{{ paper.conclusion }}
{% endif %}
{% if paper.limitations %}
局限：{{ paper.limitations | join('；') }}
{% endif %}

{% endfor %}
{% if context %}

## 相关原文片段

以下是从文献原文检索到的片段，可用于支撑具体论断：

{{ context }}
{% endif %}

# 前端构建环境变量示例。
# 本地开发无需配置（Vite 代理 /api 到 localhost:8000）。
# 生产部署到 Cloudflare Pages 时，在 Pages 项目设置里配置这些变量。

# 后端 API 基址。前后端不同源时必填，例如后端部署在 Fly.io：
#   VITE_API_BASE=https://paper-assistant.fly.dev/api/v1
# 留空时使用相对路径 /api/v1（仅当前后端同源时可用）
VITE_API_BASE=
